# docker-compose.yml의 cloudflared 서비스가 사용하는 터널 토큰.
# Cloudflare Zero Trust 대시보드 → Networks → Tunnels → Create a tunnel
# → Connector: Docker 선택 시 나오는 토큰 값 (긴 문자열)을 그대로 붙여넣는다.
CLOUDFLARE_TUNNEL_TOKEN=
